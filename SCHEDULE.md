# 📅 جدول النشر اليومي

## التوقيت الحالي للنشر

النظام مضبوط على نشر **بوست واحد** في كل من الأوقات التالية:

### التوقيت العالمي (UTC)
| الوقت (UTC) | الوقت بتوقيت القاهرة (UTC+2)* | الوصف |
|-------------|------------------------------|-------|
| 07:00 | 09:00 صباحاً | البوست الصباحي |
| 11:00 | 01:00 ظهراً | بوست منتصف النهار |
| 15:00 | 05:00 مساءً | بوست بعد الظهر |
| 19:00 | 09:00 مساءً | البوست المسائي |
| 23:00 | 01:00 بعد منتصف الليل | البوست الليلي |

**المجموع: 5 بوستات يومياً موزعة على مدار اليوم**

> ⚠️ **ملاحظة مهمة**: التوقيت المستخدم في GitHub Actions هو UTC. إذا كنت في منطقة زمنية مختلفة، احسب الفرق بينها وبين UTC لمعرفة الأوقات الفعلية.

---

## 🌍 كيفية تعديل التوقيت حسب منطقتك

### أمثلة لمناطق زمنية مختلفة:

**مصر والسعودية (UTC+3):**
```yaml
schedule:
  - cron: '0 6 * * *'   # 9 صباحاً
  - cron: '0 10 * * *'  # 1 ظهراً
  - cron: '0 14 * * *'  # 5 عصراً
  - cron: '0 18 * * *'  # 9 مساءً
  - cron: '0 22 * * *'  # 1 صباحاً
```

**الإمارات (UTC+4):**
```yaml
schedule:
  - cron: '0 5 * * *'   # 9 صباحاً
  - cron: '0 9 * * *'   # 1 ظهراً
  - cron: '0 13 * * *'  # 5 عصراً
  - cron: '0 17 * * *'  # 9 مساءً
  - cron: '0 21 * * *'  # 1 صباحاً
```

**المغرب (UTC+1):**
```yaml
schedule:
  - cron: '0 8 * * *'   # 9 صباحاً
  - cron: '0 12 * * *'  # 1 ظهراً
  - cron: '0 16 * * *'  # 5 عصراً
  - cron: '0 20 * * *'  # 9 مساءً
  - cron: '0 0 * * *'   # 1 صباحاً
```

---

## ⚙️ تخصيص الجدول الزمني

### تغيير عدد مرات النشر اليومية

#### 3 مرات يومياً:
```yaml
schedule:
  - cron: '0 9 * * *'   # 9 صباحاً
  - cron: '0 15 * * *'  # 3 عصراً
  - cron: '0 21 * * *'  # 9 مساءً
```

#### 7 مرات يومياً (كل 3 ساعات تقريباً):
```yaml
schedule:
  - cron: '0 6 * * *'   # 6 صباحاً
  - cron: '0 9 * * *'   # 9 صباحاً
  - cron: '0 12 * * *'  # 12 ظهراً
  - cron: '0 15 * * *'  # 3 عصراً
  - cron: '0 18 * * *'  # 6 مساءً
  - cron: '0 21 * * *'  # 9 مساءً
  - cron: '0 0 * * *'   # منتصف الليل
```

#### 10 مرات يومياً (كل ساعتين تقريباً):
```yaml
schedule:
  - cron: '0 7,9,11,13,15,17,19,21,23,1 * * *'
```

---

## 🎯 استراتيجيات توقيت مقترحة

### للمحتوى الترفيهي:
- 8 صباحاً (في الطريق للعمل)
- 1 ظهراً (وقت الغداء)
- 6 مساءً (بعد العمل)
- 9 مساءً (وقت الاسترخاء)
- 11 مساءً (قبل النوم)

### للمحتوى الإخباري:
- 6 صباحاً
- 9 صباحاً
- 12 ظهراً
- 3 عصراً
- 6 مساءً

### للمحتوى التعليمي:
- 7 صباحاً
- 10 صباحاً
- 2 ظهراً
- 5 مساءً
- 8 مساءً

---

## 📊 كيفية معرفة أفضل أوقات النشر

يمكنك استخدام Facebook Insights لمعرفة متى يكون جمهورك أكثر تفاعلاً:

1. اذهب إلى صفحتك على Facebook
2. اختر Insights / إحصائيات
3. اذهب إلى Posts / المنشورات
4. انظر إلى "When Your Fans Are Online"

ثم عدّل الأوقات في ملف `.github/workflows/main.yml` حسب هذه البيانات.

---

## 🔧 كيفية تطبيق التغييرات

1. افتح ملف `.github/workflows/main.yml`
2. عدّل أوقات الـ `cron`
3. احفظ الملف
4. ارفعه على GitHub:
```bash
git add .github/workflows/main.yml
git commit -m "Update posting schedule"
git push
```

سيتم تطبيق الجدول الجديد تلقائياً في اليوم التالي!

---

## 📝 فهم صيغة Cron

```
* * * * *
│ │ │ │ │
│ │ │ │ └─── يوم الأسبوع (0-6) (الأحد=0)
│ │ │ └───── الشهر (1-12)
│ │ └─────── يوم الشهر (1-31)
│ └───────── الساعة (0-23)
└─────────── الدقيقة (0-59)
```

### أمثلة:
- `0 9 * * *` = كل يوم الساعة 9 صباحاً
- `30 14 * * *` = كل يوم الساعة 2:30 عصراً
- `0 9 * * 1` = كل يوم اثنين الساعة 9 صباحاً
- `0 9 1 * *` = كل أول يوم في الشهر الساعة 9 صباحاً

يمكنك استخدام [crontab.guru](https://crontab.guru/) لاختبار صيغ الـ cron!

---

✅ **الإعداد الحالي: 5 بوستات يومياً موزعة بشكل متوازن على مدار اليوم**
